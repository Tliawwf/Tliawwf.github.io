# Git 常用指令速查表（Markdown 精简版）

---

## 一、基础配置（首次使用必看）
```bash
# 配置用户名（GitHub/Gitee 用户名）
git config --global user.name "你的用户名"

# 配置邮箱（GitHub/Gitee 绑定邮箱）
git config --global user.email "你的邮箱"

# 查看全局配置
git config --global --list
```

---

## 二、仓库克隆（拉取远程代码到本地）
```bash
# 1. 克隆仓库到默认文件夹
git clone 仓库地址（地址格式：https://github.com/用户名/仓库名.git）

# 2. 克隆仓库到指定文件夹
git clone 仓库地址 文件夹名（文件夹必须为空）

# 3. 克隆到当前空文件夹
git clone 仓库地址 .
```

---

## 三、本地核心操作（写代码必用）
```bash
# 查看文件修改状态
git status

# 暂存所有修改文件
git add .

# 暂存单个文件
git add 文件名

# 提交修改（添加备注）
git commit -m "提交说明"

# 跳过暂存，直接提交已修改的文件
git commit -a -m "提交说明"
```

---

## 四、分支管理（GitHub 核心功能）
```bash
# 查看所有分支
git branch

# 创建新分支
git branch 分支名

# 切换分支
git checkout 分支名

# 创建并直接切换到新分支
git checkout -b 分支名

# 合并分支（将指定分支合并到当前分支）
git merge 分支名

# 删除本地分支
git branch -d 分支名
```

---

## 五、远程仓库同步（上传/下载代码）
```bash
# 拉取远程最新代码
git pull

# 推送本地代码到远程
git push

# 首次推送分支（绑定远程分支）
git push -u origin 分支名

# 查看远程仓库地址
git remote -v
```

---

## 六、撤销/回滚操作（改错代码急救）
```bash
# 撤销未暂存的文件修改（恢复原样）
git restore 文件名

# 撤销已暂存的文件（从暂存区撤回）
git restore --staged 文件名

# 撤销上一次提交（未推送到远程）
git commit --amend
```

---

## 七、查看信息（日志/状态）
```bash
# 查看提交历史（精简版）
git log --oneline

# 查看当前分支状态
git status

# 查看文件具体修改内容
git diff
```

---

## 八、常用小技巧
```bash
# 临时保存未完成的代码
git stash

# 恢复临时保存的代码
git stash pop

# 刷新 DNS/重置网络代理（解决连接失败）
git config --global --unset http.proxy
git config --global --unset https.proxy
```

---

### 新手必记
1. **所有 Git 命令必须以 `git` 开头**
2. 日常流程：`git add .` → `git commit -m "备注"` → `git push`
3. 分支操作：先切分支，再写代码，最后合并